NODE_ENV=development
PORT=5000

# PostgreSQL - Configuration Docker Local (Port 5433)
# Format URL (Recommandé pour éviter les erreurs de format de mot de passe)
DATABASE_URL=postgresql://neondb_owner:npg_OL0WAwoeCDR6@ep-morning-moon-amlodrzn-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require

# Variables individuelles (utilisées par certains scripts)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=mediconnect_db
DB_USER=postgres
DB_PASSWORD=Remi2026

# JWT
JWT_SECRET=votre_secret_jwt_tres_long_et_securise
JWT_EXPIRES_IN=7d

# URLs de l'écosystème
FRONTEND_URL=http://localhost:3000
MOBILE_URL=http://localhost:19000
RDV_SITE_URL=http://localhost:3001

# Configuration Uploads
MAX_FILE_SIZE=10mb
UPLOAD_DIR=uploads/
