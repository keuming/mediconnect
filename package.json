{
  "name": "mediconnect-root",
  "private": true,
  "version": "2.0.0",
  "description": "MediConnect v2 — Gestion centralisée",
  "workspaces": [
    "backend",
    "frontend",
    "mobile",
    "rdv-site"
  ],
  "scripts": {
    "install-all": "npm install",
    "dev-backend": "npm run dev -w backend",
    "dev-frontend": "npm run dev -w frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}